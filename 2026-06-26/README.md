# 2026-06-26 학습 정리
- Kotlin에서 `var`/`val` 차이와 타입 엄격성(예: Int를 Double에 직접 대입 불가)을 학습했습니다.
- 숫자/문자 타입(`Double`, `Float`, `Long`, `Short`) 선언 규칙과 형변환(`toInt()`) 사용법을 연습했습니다.
- `lateinit`의 사용 조건(참조형 `var`만 가능, 기본형/`val` 불가)을 확인했습니다.
- 함수 선언과 반환 타입(`Int`, `Unit`) 및 기본 함수 호출 패턴을 실습했습니다.
- `Any` 타입으로 여러 자료형을 담는 방법과 간단한 출력 예제를 통해 동작을 확인했습니다.